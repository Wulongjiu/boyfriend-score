import type { DimensionId } from './types';

/**
 * 维度 → 兜底建议库
 *
 * 定位说明（v2）：这不是主结论。主结论由**关系原型**（lib/archetypes.ts）给出，
 * 因为它能结合多个维度说清具体处境。这里只在两种情况下兜底：
 *   1. 作答太少，没有任何原型条件被满足；
 *   2. 命中的原型不足 3 个时，用维度建议补足。
 *
 * 撰写规范：每条建议必须独立成立、可单独阅读，不依赖上下文；
 * 不要求她"更懂事"，只给可执行的下一步。
 */
export const DIMENSION_ADVICE: Record<DimensionId, readonly string[]> = {
  responsiveness: [
    '下次你难受的时候，直接说出需求："我现在不需要建议，我就想被听一会儿。"把需求讲成他能执行的样子，是给这段关系一个机会，不是妥协。',
    '留意他回应的对象：他是在回应你说的事情，还是在回应你的情绪。前者是解决问题，后者才是被在乎——很多关系卡在这个区别上。',
  ],
  conflict: [
    '吵完之后注意"谁先回来"。如果一直是你先低头，那这段关系的修复工作全靠你一个人在扛，这比吵架本身更消耗。',
    '下一轮冲突里，把"你总是……"换成一个具体时刻："那天你那样说的时候，我很难受。"具体的事他能回应，泛化的指责只会引来防御。',
  ],
  consistency: [
    '把他说过但没做到的事挑最近三件写下来。三件里有两件没兑现，问题就不在"他忘了"，而在这段关系里承诺不值钱。',
    '以后别只接受口头承诺。他说"下次带你去"，你回一句"那我们定个具体时间"。能定下来的才算承诺。',
  ],
  boundary: [
    '让你安心是他的责任，不是你的过度敏感。需要反复追问才肯保持距离的关系，会让你长期处在警戒状态。',
    '如果你为某个异性表达过不舒服而他没变化，看行为不看解释。愿意处理的人会主动拉开距离，不愿意的会说"你管太多"。',
  ],
  load: [
    '选一件具体的事（下次出行、下次家庭聚会）整件事交给他，包括"记得"。不要中途接手，也不要在旁边提醒——只有真交出去，才能看清谁在操心。',
    '把"我最近很累，因为家里的安排都在我脑子里"说出来。很多男性不是不愿分担，是从没意识到记和安排也算工作。',
  ],
  future: [
    '自然地问一次未来（比如明年怎么安排），注意他句子里的主语是"我"还是"我们"。这个细节比承诺的大小更能说明问题。',
    '承诺不用宏大，但要具体。只谈感觉不谈安排的未来，通常只是当下的安抚。',
  ],
  integration: [
    '看他会不会主动把你介绍给他的朋友和家人。被藏起来的关系，通常有它被藏起来的原因。',
    '他在家人面前怎么介绍你、怎么维护你，是他对这段关系定位的直接体现——这比私下对你多好更能说明问题。',
  ],
};

/**
 * 取指定维度中得分最低的几个维度，返回对应的兜底建议
 * @param weakestIds 已按分数升序排列的维度 id
 * @param limit 最多返回几条
 */
export function getAdviceForDimensions(
  weakestIds: readonly DimensionId[],
  limit = 3,
): string[] {
  const advice: string[] = [];
  for (const id of weakestIds) {
    const list = DIMENSION_ADVICE[id];
    if (!list) continue;
    for (const item of list) {
      if (advice.length >= limit) return advice;
      advice.push(item);
    }
  }
  return advice;
}
